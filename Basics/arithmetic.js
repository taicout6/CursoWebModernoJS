const [a, b, c, d] = [3, 5, 1, 15];
const sum = a + b + c + d;
const subtract = b - c;
const multiply = b * d;
const divide = d / b;
const modulo = d % b;

console.log(sum, subtract, divide, modulo);
